# منطق حساب عدد الاختبارات

## القاعدة الأساسية
**لا تُحسب الاختبارات التي تقديرها "إعادة" ضمن عدد الاختبارات**

## الأمثلة

### مثال 1: طالب عمل اختبار واحد
- الاختبار الأول: تقدير "إعادة" ❌
- **عدد الاختبارات: 0**

### مثال 2: طالب عمل 3 اختبارات
- الاختبار الأول: تقدير "ممتاز" ✅
- الاختبار الثاني: تقدير "إعادة" ❌
- الاختبار الثالث: تقدير "جيد جداً" ✅
- **عدد الاختبارات: 2**

### مثال 3: طالب عمل اختبار وأعاده
- المحاولة الأولى: تقدير "إعادة" ❌
- المحاولة الثانية: تقدير "ممتاز" ✅
- **عدد الاختبارات: 0** (المحاولة الأولى لا تُحسب)
- **عدد الإعادات: 1** (المحاولة الثانية)

## الشروط

### يُحسب الاختبار إذا:
1. ✅ التقدير **ليس** "إعادة"
2. ✅ رقم المحاولة = 1 (المحاولة الأولى)

### لا يُحسب الاختبار إذا:
1. ❌ التقدير = "إعادة"
2. ❌ رقم المحاولة > 1 (محاولة إعادة)

## الكود التقني

### في سجلات الطلاب (Teacher & Admin)
```typescript
// استثناء الاختبارات التي تقديرها "إعادة"
examData?.forEach(exam => {
  const summary = studentMap.get(exam.student_id);
  if (summary && exam.grade !== 'إعادة') {
    summary.examsCount++;
  }
});
```

### في السجلات التفصيلية
```typescript
// استثناء بناءً على المحاولة والتقدير
const studentExams = examsData?.filter(e => 
  e.student_id === studentId && 
  e.attempt_number === 1 &&        // المحاولة الأولى فقط
  e.grade !== 'إعادة'              // ليس إعادة
) || [];
const examsCount = studentExams.length;
```

## الصفحات المتأثرة

### 1. سجلات الطلاب (الأستاذ)
- `src/pages/StudentRecords.tsx`
- 3 أماكن تم تحديثها

### 2. سجلات الطلاب (الأدمن)
- `src/pages/AdminStudentRecords.tsx`
- 3 أماكن تم تحديثها

## نقاط الترتيب

### الحساب:
```
نقاط الترتيب = الصفحات المسمعة + (عدد الاختبارات × 2)
```

### مثال:
- طالب سمّع 50 صفحة
- عمل 3 اختبارات (1 منها إعادة)
- **عدد الاختبارات المحسوبة: 2**
- **نقاط الترتيب: 50 + (2 × 2) = 54**

## الفوائد

### 1. العدالة ⚖️
- الطالب الذي يأخذ "إعادة" لا يُحسب له اختبار
- يشجع على الاستعداد الجيد

### 2. الدقة 🎯
- الإحصائيات تعكس الاختبارات الناجحة فقط
- سهولة تتبع التقدم الفعلي

### 3. الوضوح 📊
- الأرقام واضحة ومفهومة
- لا لبس في العد

## ملاحظات مهمة

### الإعادات (Retakes)
- تُحسب بشكل منفصل في عمود "الإعادات"
- لا تؤثر على عدد الاختبارات الأساسي

### المحاولات المتعددة
- فقط المحاولة الأولى تُحسب (إذا لم تكن إعادة)
- المحاولات الإضافية تُحسب كـ "إعادات"

### التقارير
- في تقرير الطالب: تظهر جميع الاختبارات (بما فيها الإعادات)
- في السجلات: تُحسب فقط الاختبارات الناجحة

## الخلاصة

✅ **الاختبارات الناجحة فقط تُحسب**
❌ **الاختبارات بتقدير "إعادة" لا تُحسب**
📊 **الإحصائيات دقيقة وعادلة**
